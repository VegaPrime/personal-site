# Copy this file to .env.local and fill in your key.
# The sync-resume script uses this to parse resume.pdf via Claude API.
# Without it, npm run build uses the last committed data/resume.ts.
ANTHROPIC_API_KEY=sk-ant-...
